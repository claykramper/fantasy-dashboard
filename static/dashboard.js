let dashboardData=null;let selectedSlates=new Set();let showCompleted=false;let selectedWeekKey=null;

async function loadDashboard(){try{const r=await fetch('/api/dashboard',{cache:'no-store'});const d=await r.json();if(!r.ok)throw new Error(d.error||'Dashboard error');dashboardData=d;initializeSelectedSlates();renderDashboard()}catch(e){document.getElementById('content').innerHTML=`<div class="error">${escapeHtml(e.message)}</div>`}}

function initializeSelectedSlates(){const weekKey=dashboardData.fantasy_week_key||'';if(selectedWeekKey!==weekKey){selectedWeekKey=weekKey;selectedSlates.clear();showCompleted=false}if(selectedSlates.size===0&&dashboardData.next_slate_id!==null)selectedSlates.add(dashboardData.next_slate_id)}

function toggleSlate(id){selectedSlates.has(id)?selectedSlates.delete(id):selectedSlates.add(id);renderDashboard()}
function toggleCompleted(){showCompleted=!showCompleted;renderDashboard()}

function renderDashboard(){document.getElementById('content').innerHTML=`${renderSlateSelector()}<div class="refresh-row"><div class="updated">Live dashboard</div><button class="refresh" onclick="refreshDashboard()">↻ Refresh</button></div>${renderSleeper()}${renderESPN()}${renderYahoo()}`}

function renderSlateSelector(){if(!dashboardData.slates?.length)return'';const options=dashboardData.slates.map(s=>`<div class="slate-option"><input type="checkbox" id="slate-${s.id}" ${selectedSlates.has(s.id)?'checked':''} onchange="toggleSlate(${s.id})"><label for="slate-${s.id}">${escapeHtml(s.label)}</label></div>`).join('');return `<div class="slate-card"><div class="slate-label">NFL SLATES</div><div class="slate-options">${options}<div class="slate-option completed-option"><input type="checkbox" id="games-completed" ${showCompleted?'checked':''} onchange="toggleCompleted()"><label for="games-completed">Games Completed</label></div></div></div>`}

function playerGameHasFinished(p){const s=String(p.game_status||'').toUpperCase();const d=String(p.status_detail||'').toUpperCase();return s.includes('FINAL')||s.includes('COMPLETED')||d.includes('FINAL')||d.includes('END')}
function playerGameIsLive(p){const s=String(p.game_status||'').toUpperCase();const d=String(p.status_detail||'').toUpperCase();return !playerGameHasFinished(p)&&(s.includes('LIVE')||s.includes('IN_PROGRESS')||d.includes('LIVE'))}
function playerBelongsToSelectedSlate(p){if(showCompleted&&playerGameHasFinished(p))return true;if(!p.pro_team_id||selectedSlates.size===0)return false;const t=String(p.pro_team_id).toUpperCase();for(const id of selectedSlates){const slate=dashboardData.slates.find(x=>x.id===id);if(!slate)continue;for(const g of slate.games){if([g.home_id,g.away_id,g.home,g.away].some(v=>String(v).toUpperCase()===t))return true}}return false}

function renderSleeper(){return dashboardData.sleeper?renderLeagueSection(dashboardData.sleeper,'Sleeper'):`<div class="league-section"><div class="error">${escapeHtml(dashboardData.sleeper_error||'Sleeper data unavailable.')}</div></div>`}
function renderESPN(){return dashboardData.espn?renderLeagueSection(dashboardData.espn,'ESPN'):`<div class="league-section"><div class="error">${escapeHtml(dashboardData.espn_error||'ESPN data unavailable.')}</div></div>`}

function renderYahoo(){const y=dashboardData.manual_yahoo;if(!y){return `<div class="league-section"><div class="score-card yahoo"><div class="matchup single-score"><div><div class="team-abbrev">YAHOO</div><div class="score">--</div><div class="score-secondary">Projection unavailable</div></div></div></div><div class="error">${escapeHtml(dashboardData.manual_yahoo_error||'Manual Yahoo roster unavailable.')}</div></div>`}
const visibleStarters=(y.starters||[]).filter(p=>playerBelongsToSelectedSlate(p));
const visibleAlternatives=(y.alternatives||[]).filter(p=>playerBelongsToSelectedSlate(p));
return `<div class="league-section"><div class="league-subtitle">${escapeHtml(y.league_name||'Yahoo')}</div><div class="score-card yahoo"><div class="matchup single-score"><div><div class="team-abbrev">${escapeHtml(y.league_name||'MY TEAM')}</div><div class="score">${formatNumber(y.projected_total)}</div><div class="score-secondary">Projected lineup</div><div class="score-secondary">10 starters</div></div></div></div><div class="yahoo-lineup"><div class="yahoo-lineup-title">Projected lineup</div>${visibleStarters.length?visibleStarters.map(renderYahooPlayer).join(''):`<div class="no-players">No projected starters in selected slate</div>`}${renderYahooAlternatives(visibleAlternatives)}</div>${renderYahooUnmatched(y.unmatched)}</div>`}

function renderYahooPlayer(p){const game=p.opponent&&p.game_time?`<span class="game-info">${escapeHtml(p.nfl_team||p.team)} vs ${escapeHtml(p.opponent)} · ${escapeHtml(p.game_time)}</span>`:p.nfl_team?`<span class="game-info">${escapeHtml(p.nfl_team)}</span>`:'';const finished=playerGameHasFinished(p);const live=playerGameIsLive(p);const projection=p.projected!=null?p.projected:p.projection;const proj=projection!=null?Number(projection).toFixed(2):'--';const actual=p.actual!=null?Number(p.actual).toFixed(2):'--';let pointsHtml;if(finished){pointsHtml=`<div class="yahoo-projection"><span class="actual-points">${actual}</span><div class="points-label">ACTUAL</div></div>`}else if(live){pointsHtml=`<div class="yahoo-projection"><span class="live-actual">${actual}</span><div class="points-secondary projected-points">${proj} PROJ</div></div>`}else{pointsHtml=`<div class="yahoo-projection"><span class="projected-points">${proj}</span><div class="points-label">PROJ</div></div>`}const state=finished?'FINAL':live?'LIVE':'PROJ';const cls=finished?'locked':live?'live':'projected';return `<div class="yahoo-player"><div class="yahoo-slot">${escapeHtml(p.slot)}</div><div><div class="yahoo-player-name">${escapeHtml(p.name)}</div><div class="player-meta"><span class="status ${cls}">${state}</span>${game}</div></div>${pointsHtml}</div>`}

function renderYahooAlternatives(items){if(!items||!items.length)return'';return `<div class="yahoo-alt-heading">Close alternatives</div>${items.map(p=>{const projection=p.projected!=null?p.projected:p.projection;return `<div class="yahoo-player yahoo-alternative"><div class="yahoo-slot">${escapeHtml(p.slot)}</div><div><div class="yahoo-player-name">${escapeHtml(p.name)}*</div><div class="player-meta"><span class="game-info">Within 1.5 of ${escapeHtml(p.close_to)}</span></div></div><div class="yahoo-projection">${formatNumber(projection)}</div></div>`}).join('')}`}
function renderYahooUnmatched(items){if(!items||!items.length)return'';return `<div class="pending yahoo-unmatched"><strong>Could not match:</strong> ${items.map(escapeHtml).join(', ')}</div>`}

function renderLeagueSection(league,platform){const my=league.my_team,opp=league.opponent;return `<div class="league-section"><div class="league-subtitle">${escapeHtml(league.league_name||`Week ${league.week}`)}</div><div class="score-card ${platform.toLowerCase()}"><div class="matchup"><div><div class="team-abbrev">${escapeHtml(my.abbrev)}</div><div class="score">${formatNumber(my.actual_total??my.total??0)}</div><div class="score-secondary">Actual</div><div class="score-secondary">Projected: ${formatNumber(my.projected_total??my.total??0)}</div></div><div class="vs">VS</div><div><div class="team-abbrev">${escapeHtml(opp.abbrev)}</div><div class="score">${formatNumber(opp.actual_total??opp.total??0)}</div><div class="score-secondary">Actual</div><div class="score-secondary">Projected: ${formatNumber(opp.projected_total??opp.total??0)}</div></div></div></div><div class="rosters">${renderTeam(my,league,false)}${renderTeam(opp,league,true)}</div></div>`}

function renderTeam(team,league,isOpponent){const players=(team.players||[]).filter(playerBelongsToSelectedSlate);return `<div class="team-card${isOpponent?' opponent-roster':''}">${players.length?players.map(renderPlayer).join(''):`<div class="no-players">No starters in selected slate</div>`}</div>`}

function renderPlayer(p){const proj=p.projected!=null?Number(p.projected).toFixed(2):'--';const actual=p.actual!=null?Number(p.actual).toFixed(2):'--';const finished=playerGameHasFinished(p);const live=!finished&&(playerGameIsLive(p)||String(p.status||'').toUpperCase()==='LIVE'||p.actual!=null);let pts=finished?`<div class="points"><span class="actual-points">${actual}</span><div class="points-label">ACTUAL</div></div>`:live?`<div class="points"><span class="live-actual">${actual}</span><div class="points-secondary projected-points">${proj} PROJ</div></div>`:`<div class="points"><span class="projected-points">${proj}</span><div class="points-label">PROJ</div></div>`;let injury='';if(p.injury&&p.injury.toUpperCase()!=='ACTIVE'){const c=['OUT','DOUBTFUL'].includes(p.injury.toUpperCase())?'injury-alert':'';injury=`<span class="injury ${c}">${escapeHtml(p.injury)}</span>`}let game='';if(p.nfl_team&&p.opponent&&p.game_time)game=`<span class="game-info">${escapeHtml(p.nfl_team)} vs ${escapeHtml(p.opponent)} · ${escapeHtml(p.game_time)}</span>`;else if(p.nfl_team)game=`<span class="game-info">${escapeHtml(p.nfl_team)}</span>`;const cls=finished?'locked':live?'live':'projected';const st=finished?'✓ FINAL':live?'● LIVE':'PROJECTED';return `<div class="player"><div class="position">${escapeHtml(p.slot)}</div><div><div class="player-name">${escapeHtml(p.name)}</div><div class="player-meta"><span class="status ${cls}">${st}</span>${injury}${game}</div></div>${pts}</div>`}

function refreshDashboard(){loadDashboard()}
function formatNumber(v){const n=Number(v);return Number.isFinite(n)?n.toFixed(2):'0.00'}
function escapeHtml(v){if(v==null)return'';return String(v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;')}

loadDashboard();setInterval(loadDashboard,60000);
